# 误判分类与项目高频表

> Layer 1（六类泛化模式）已迁移至 `skills/dcorr/SKILL.md` 第 72-128 行，以四列表格完整展开。

## 项目高频误判表机制

存储位置：`.diwu/project-pitfalls.md`（项目本地维护）

### 完整生命周期

| 阶段 | 动作 | 触发位置 |
|------|------|---------|
| **创建** | 从 `project-pitfalls.md.template` 初始化 | `/dinit` 命令 |
| **读取** | Preflight 第 4 步预加载历史误判 | `mindset.md` + `drun` SKILL.md |
| **追加** | 归档时写入新踩坑条目 | `drec` SKILL.md + `darc` SKILL.md |
| **维护** | 定期回顾和清理过时条目 | 人工或归档触发 |

### 格式规范

- 按 `## Source:` 分隔符追踪所属 session
- 每条标注来源 session 文件名（如 `session-2026-04-18-213522.md`）
- 禁止写归档文件名或占位符
- 消费顺序：先查 `dcorr` 泛化模式 → 再查本项目高频表 → 再做判断

---

## 接口预留

| 预留 ID | 用途 | 状态 |
|---------|------|------|
| P-001 | 新泛化模式类型（超出当前六类） | 待定义 |
| P-002 | 跨项目知识库同步 | 待定义 |
| P-003 | 自动检测告警 | 待定义 |
| P-004 | 跨 session 错误模式注入 | 待实现（inject_errors_decisions.py） |

发现无法归入现有六类的误判模式时，先记入项目高频表，积累足够样本后再评估是否提升为泛化新类别。
