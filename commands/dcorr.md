---
description: "纠偏恢复触发器——偏航时启动五步纠偏协议"
argument-hint: [偏航描述（可选）]
allowed-tools: Read, Glob, Bash
effort: low
---

# /dcorr — 纠偏恢复

> **定位**：偏航时的恢复协议触发器，不是完整规范。完整方法论见 **`skills/dcorr/SKILL.md`**。
> **目标**：把"继续补说明硬推"改成"先回到主线，再继续执行"。

## 触发条件

当出现以下任一情况时，停止扩写，启动本命令：

- 同一问题已经被纠偏两次以上，仍沿旧路径推进
- 同一个前提被反复解释
- 输出越来越像通用套路，越来越不像当前任务
- 讨论越来越长，但下一步动作越来越模糊
- 已经改动，却拿不出运行态或输出层证据
- 开始同时引用多个目录、多个入口、多个"真相源"

## 执行

运行 `/dcorr` 后按 **`skills/dcorr/SKILL.md`** 五步协议依次执行：

1. **停止当前动作**（§退化信号）— 先停掉 6 类偏航动作
2. **四行重写**（§四行重写模板）— 用 目标/主线/现象/缺口 重新锚定
3. **误判排查**（§误判排查表）— 按六类泛化模式排除第一判断错误
4. **入口重判**（§入口重判门控）— 重新评估不确定性门控 A/B/C
5. **恢复执行**（§恢复执行骨架）— 按 P-J-A 骨架输出现象→判断→动作

> 不确定性与门控决策详见 `rules/mindset.md` §不确定性门控。验证优先级（L1-L5）详见 `rules/verification.md`。
