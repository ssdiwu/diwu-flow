# diwu-flow 可调参数配置
# 修改后立即生效，无需重启

task_archive_limit = 20
recording_file_limit = 30
recording_retention_days = 30
dloop_review_cap = 5
dloop_max_consecutive = 0
context_monitor_warning = 30
context_monitor_critical = 50
context_monitor_delay = 10
error_tracking_enabled = true

[drift_detection]
enabled = true

[recording_reminder]
enabled = true
