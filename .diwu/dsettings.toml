# diwu-flow 可调参数配置
# 修改后立即生效，无需重启

task_archive_limit = 20
recording_file_limit = 30
recording_keep_days = 30
dloop_review_cap = 5
dloop_max_consecutive = 0
ctxmon_warn_at = 30
ctxmon_checkpoint_at = 50
ctxmon_checkpoint_delay = 10
error_tracking_enabled = true
drift_enabled = true
reminder_on_taskdone = true
