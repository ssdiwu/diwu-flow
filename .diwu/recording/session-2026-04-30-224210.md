## Session 2026-04-30 22:42:10
### Task#32: 激活 decisions.md：集成到 /drec + 门槛分档 + 三层软强制 → Done
**实施内容**:
- `skills/drec/SKILL.md`：在 Session 文件格式模板与踩坑经验之间新增「设计决策记录（Session 结束检查点）」章节，含两档判断标准表（必须写/建议写）、3 步自检流程、追加格式模板
- `rules/judgments.md` + `.claude/rules/judgments.md` + `assets/dinit/assets/rules/judgments.md`：三副本同步将「何时写 decisions.md」从单档（正例/反例/边界例）改为两档表格（必须写/建议写/不写），并追加 drec 交叉引用
- `hooks/scripts/stop_decision.py`：新增 `_check_decision_reminder(cwd)` 函数，扫描最近 2 个 session recording 检测决策关键词；在 `decide_default_mode` session 结束路径注入 stderr 轻提示；不改变 exit code 和 block/continue 行为
- `scripts/dstat.py`：空决策时显示 `(暂无设计决策记录)` 替代原来的 `- 无决策记录`
- `.diwu/decisions.md`：写入首条决策记录——dtask-state.json 统一 runtime state 真相源架构决策（备选 A/B → 选定 B → 影响 Task#26-31）
- `skills/djug/SKILL.md`：同步两档格式 + drec 交叉引用

**验收验证**:
- [x] drec SKILL.md 含"设计决策记录（Session 结束检查点）"章节且含两档标准 (L3: grep 命中)
- [x] judgments.md 三副本（rules/.claude/assets）均为两档表格格式 (L3: grep 三副本均命中)
- [x] stop_decision.py 含 `_check_decision_reminder` 且 session 结束时调用 (L3: 函数存在 + 测试 11 passed)
- [x] dstat 运行输出：decisions.md 有内容时展示决策条目 (L1: 实际运行输出确认)
- [x] djug SKILL.md 两档格式 + drec 交叉引用 (L3: grep 命中)
- [x] 全量回归通过（pytest tests -q → 226 passed）(L3)

**提交**: 待 commit
### 下一步: 处理归档（30 Done ≥ 阈值 20）+ commit + push origin/main
### 本次踩坑/经验

- [分层未拆清] Edit 工具对 `/Users/diwu/.claude/plugins/marketplaces/ssdiwu/skills/djug/SKILL.md` 的多行中文匹配反复失败（5 次），但 Read 工具能正确显示内容、Python 脚本能正确 replace → 误判为 Edit 工具的字符串匹配有 bug → 正确做法：该文件可能有不可见的编码层面差异（如 BOM 零宽字符等），改用 Python 脚本做精确字节级替换更可靠；单行 Edit 匹配可用但多行中文块在该文件上不稳定
- [验证误读] 在 `decide_default_mode` 中引用了 `cwd` 变量但该函数签名没有此参数 → NameError 导致 5 个测试失败 → 误判为逻辑错误实际是参数传递遗漏 → 正确做法：改完函数内部变量依赖后立即跑定向测试，不等到全量回归
