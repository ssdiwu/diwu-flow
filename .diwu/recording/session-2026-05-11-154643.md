## Session 2026-05-11 15:46:43
### Task#135: dtask.json / dtask-state.json → TOML 迁移 → Done（本轮修复）
**实施内容**:
- 调查 dtask.toml / dtask-state.toml 内容稀少且全英文的根因：TOML 迁移发生在归档清空之后，AI 用英文硬编码重写了 Task#135
- 从 git history 恢复 Task#135 完整中文定义到 dtask.toml
- Issue #49 context_monitor 全面重构：
  - 缓存路径从 `.diwu/` 迁移至 `tempfile.gettempdir()` + session ID 命名空间（跨平台零 git 污染）
  - 输出格式从普通 JSON 改为 `additionalContext`（AI + 用户均可见）
  - 砍掉 checkpoint 文件写入逻辑（无消费者，设计未落地）
  - 加 stale cleanup（1 小时过期自动清理）
- 砍掉 drift_detect_pre.py（方案 A）：删除脚本/测试/hooks.json 注册，创建 #52 留待后续重构
- task.toml.template 和 .diwu/dtask.toml 顶部加中文注释（示例 task + 字段说明）
- .gitignore 删除 context_monitor_cache 条目
- 测试同步更新：test_context_monitor_dict.py 改 tempdir 断言、删 checkpoint 测试；test_task_165 删旧路径断言

**验收验证**: - [x] L2 test_context_monitor_dict 9/9 PASS — [pytest]
- [x] L3 全量回归 381/381 PASS — [pytest]
- [x] dtask.toml TOML 解析验证通过 — [tomllib.load]
- [x] 归档执行正常 — [drec_archive run]

### 下一步:
- #51 归档文件 TOML 迁移待实施
- #52 drift_detect_pre 重构（方案 B）待后续迭代

### 本次踩坑/经验
- [读层现象] TOML 迁移时序 bug 表象为"内容被英文重写"，实际根因是归档先于迁移执行导致空文件转换 + AI 行为问题——需要从 git history 多个 commit 追踪时序才能定位
- [路由护栏契约] Hook 输出 JSON 字段名决定是否注入 AI context：`additionalContext` 和 `additionalSystemPrompt` 是 Claude Code 识别的两个注入字段，自定义字段（如 `level/message`）只到终端不到 AI
- [分层未拆清] 功能评估需区分"代码存在"和"链路通":context_monitor 的 checkpoint 写入了但无任何消费者读取，drift_detect_pre 的阈值设置脱离实际行为模式导致从未触发——两者都是半成品
