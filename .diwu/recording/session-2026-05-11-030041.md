## Session 2026-05-11 03:00:41
### Task#135: dtask.json / dtask-state.json → TOML 迁移 → Done
**实施内容**: 
- Layer 1: 基础设施 — common.py 路径常量 DTASK_JSON→DTASK_TOML, _shared.py 新增 load_toml_fallback(), dtask_state.py _read_json→TOML/save_runtime_state→save_toml/_remove_none()
- Layer 2: Python 消费者 — dtask_transition.py/dloop.py/dstat.py/drec_archive.py/dinit.py 全部从 load_json_or_empty/save_json 切换到 load_toml_or_empty/save_toml
- Layer 3: Hook 脚本 — 7 个脚本路径常量/读写入 TOML，stop_decision.py 直接 tomllib.load()
- Layer 4: 测试夹具 — ~17 文件 json.dumps→tomli_w.dumps，3 类失败修复（dloop None key/stderr vs stdout/残留 JSON 引用）
- Layer 5: 文档/规则 — rules/skills/commands/.doc/ 中 ~20 文件 dtask.json→dtask.toml 全量替换
- Layer 6: 模板 — task.json.template→task.toml.template，assets 模板文件 .json→.toml
- Layer 7: 迁移兼容 — dtask_state.py _read_json 增加 JSON fallback 支持 legacy 迁移

**验收验证**:
- [x] Given pytest tests/ -q When 全量回归 Then 389 passed (L3)
- [x] Given dtask.toml/dtask-state.toml When tomllib.load() Then 解析成功 (L1)
- [x] Given python3 scripts/dstat.py --cwd . When 执行 Then ok=True, tasks total=1 (L2)
- [x] Given python3 scripts/dtask_transition.py sync-runtime When 执行 Then ok=True (L2)
- [x] Given grep "dtask\.json" in hooks/scripts/ rules/ skills/ commands/ .doc/ When search Then 零残留 (L3)

**提交**: pending
### 下一步: /drec commit + 定时任务继续提醒 dloop
### 本次踩坑/经验
- [数据缺口] TOML 迁移后 test_corrupted_dtask_toml 失败 → load_toml_optional 损坏时走 error_exit→stderr，但原测试读 stdout → 误判为功能退化 → 改测试读 stderr
- [分层未拆清] test_dtask_state 断言 state["dloop"] 失败 → _remove_none() 过滤 None 值后 TOML 无 dloop key，但测试期望直接索引 → 误判为状态丢失 → 改测试用 .get("dloop")
- [环境漂移] 5 个 assets 模板文件残留 dtask.json → Layer 5 只改了 rules/skills/commands/.doc/，遗漏 assets/ → subprocess 修复时一并清理
