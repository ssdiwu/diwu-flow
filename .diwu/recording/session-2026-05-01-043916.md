## Session 2026-05-01 04:39:16

### v0.0.6 发版后：public tags 修复 + drelease.sh 收口

**问题诊断**:
- v0.0.3 指向孤立 clean commit `e9e9a0d`（从未推送到任何 remote）→ 修复：retag 到 origin commit `92d1224`（已在 public 历史中）
- v0.0.5 tag 存在本地但从未推送到 public → 直接 `git push public v0.0.5`
- v0.0.6 public tag 已在上轮修复（指向 clean commit `13ef883`）

**实施内容**:
- 删除本地 v0.0.3（指向孤立 commit `a6dd0ff`），重建指向 `92d1224`，force push 到 origin + push 到 public
- 直接 push v0.0.5 到 public（commit `d2345a4` 已在 public 历史中）
- `drelease.sh` Step 3 重写：
  - 历史 tags 补推：遍历 local tags，找出 public 缺少的，自动补推
  - public tag 指向修正：当 clean commit 与 origin commit 不同时，用 `${VERSION}-public` 临时 tag 指向 clean commit，再 push 为 `${VERSION}`（tag 内容指向 clean，非 origin）
  - 当 clean == origin 时直接推

**验收验证**: public remote v0.0.1~v0.0.6 全部存在（L1 运行态），bash -n 语法通过（L3）

**提交**: drelease.sh 修改待 commit

### 下一步: commit drelease.sh 修改

### 本次踩坑/经验

- [数据缺口] v0.0.3 tag 指向孤立 clean commit（`e9e9a0d`），该 commit 从未推送到任何 remote，导致 public 上看不到这个 tag → 原因是早期 drelease.sh 的 worktree 产物生成了 clean commit 但从未推送到 public → 误判为"tags 已正确"只因 origin 上有 → **正确做法**：发版后必须 `git ls-remote --tags public` 独立验证，不信任 origin 状态
- [分层未拆清] drelease.sh 原版 Step 3 只推当前版本 tag，不处理历史遗留；且 public tag 指向 origin commit 而非 clean commit（敏感文件仍可从 commit 历史中获取，虽然不在 tree 中）→ **正确做法**：public tag 必须指向 clean commit，且每次发版顺带同步缺失历史 tags
