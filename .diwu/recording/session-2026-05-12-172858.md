## Session 2026-05-12 17:28:58

### Task: #54 框架讨论 Phase 2 收口 — 正文回写 + P0/P1/P2 实施总览 + 第二轮审视回应

**实施内容**:
- A 阶段完成：4 处正文回写（§6.2 字段定义 / §10.5 协同边界 / §9.4 远程映射 / §14 Phase 0 MVP）
- B 阶段完成：P0 最小不可争议版字段收缩清单（16 必实现字段 + 3 成熟度示例 + 注释规范）
- C 阶段完成：v0.2.0 实施总览表（P0 五项 + P1 五项 + P2 五项 + 依赖图）
- 回应 RexYoung000 第二轮审视（8 个隐患 + 解决建议）
- 收敛多条关键修正：

  **状态机与远程镜像修正**:
  - Issue 创建时机从"gate READY 后"前移到"正式进入收敛链(dpth/dref)时"
  - InHold 重新定义为**待决策状态**（不是 InReview），标签「待决策」映射到 InHold 非 InReview
  - InHold→InSpec 分两种情况：定义未变(轻量复查) vs 定义实质变化(重过 gate)

  **实现规格补充**:
  - 强注释是必需不是附赠：每个字段必须回答它是什么/何时填/合法值/与相近字段区别
  - type 字段必须兼容现有 dtask.category（functional/ui/bugfix/refactor/infra），P0 不自创分类
  - migration-report 只报事实不做建议/不做 feature 边界猜测/slug 命名建议

  **dfeat 物理形态修正**:
  - dfeat 从"单个 dfeat.toml"收敛为"`/.diwu/dfeat/`**目录中多文件对象，每 feat 一文件
  - 文件名前缀规则：`[active]_*.toml` / `[hold]_*.toml` / `[done]_*.toml` / `[cancelled]_*.toml`
  - remote 板块语义修正：不再以 gate 后为前提，只表示"已拥有远程镜像"

  **状态机流转表（规则级收口版）**:
  - 完整 10 条三元组流转规则：(当前状态 → 新状态 : 触发者, 触发条件, 是否需确认)
  - 核心原则："AI 负责推进正常收敛与执行路径；人负责决定对象命运；gate 重跑与否取决于对象定义是否发生实质变化"

  **RexYoung000 第二轮关键隐患**:
  - dfeat.toml 单文件规模风险（建议多文件或归档策略，Phase 0 用 5-10 feat 压测）
  - 状态机转换触发条件不够显式（已补三元组定义）
  - execution.history gate 前断层（改为 gate 前全量保留，Issue 创建时一次性回溯归档）
  - dinit 迁移缺版本标识（待解决）

**验收验证**:
- [x] #54 正文包含 §6.2/§9.4/§10.5/§14 四处独立可读子节
- [x] #54 标签 `可实施`
- [x] 13 条评论覆盖框架+回应+收缩+总览+第二轮审视+流转表
- [x] P0/P1/P2 三级实施总览表写入，含依赖图和验证点
- [x] 状态机 10 条三元组流转规则锁定（触发者/条件/确认要求）

**提交**: commit f58e1cb (上一轮 recording) 待追加

### 下一步
- 按 P0-1 → P0-2 → ... 顺序逐个拆分子 issue 实施
- 第一个子 issue：P0-1（dfeat.toml schema + CRUD 脚本）
- 创建新标签「进行中」「待决策」（gh label create）

### 本次踩坑/经验
- [分层未拆清] .doc/ 写入模型最初设计为 feat 子目录 → 被 507 纠正为增量 patch 现有真相源文件。**教训：真相源的"唯一性"必须在设计最早期就锁死，不能留模糊空间**
- [路由护栏契约] Issue 创建时机先后两版结论（gate READY 后 vs 正式收敛链起点）→ 被 507 修正为后者。**教训：同一议题的"合理答案"可能随讨论深入而演化，不要把早期结论当终态**
- [读层现象] InHold 标签映射两版（InReview vs InHold）→ 被 507 纠正为 InHold=待决策。**教训：状态名必须先对齐人类直觉再技术化，InHold 天然更接近"暂停等决定"**
- [验证误读] 正文中多次编辑替换锚点因全角半角/空格差异未命中 → **教训：替换前必须用行号定位+raw bytes 对比确认匹配文本存在；或用行号插入替代字符串替换**
- [新] dfeat 物理形态从单文件演变为目录 → **教训：对象存储形态是实施级关键决策，应在 Phase 0 前锁定，不在实施中途演变**
- [新] type 字段分类 → **教训：兼容优先于创新，P0 不引入新分类体系**
- [新] migration-report 职责边界 → **教训：迁移工具只交还判断不替人做决策，这是防止 AI 过度推断的防火墙**
