## Session 2026-05-10 22:02:08

### 整体目标
对 PR #42 第三轮审查回复：逐条验证 507 提出的 P1/P2 问题，给出判断结论。

### 审查发现

#### 认可的 P1 问题
- **#31 D3 键名契约歪了 4 个**：`recording_retention_days` 应为 `recording_keep_days`，`ctxmon_warning_threshold` 应为 `ctxmon_warn_at`，`ctxmon_critical_threshold` 应为 `ctxmon_checkpoint_at`，`ctxmon_check_interval` 应为 `ctxmon_checkpoint_delay`。Issue 规格明确，当前模板/实例/迁移器/消费者全偏离。
- **completeness.md 停留在旧四层模型**：仍用数据层/接口层/业务逻辑层/产品层，与 Issue #24 锁定的 8 类文档类型驱动不符。ddoc 入口改路由但完备性检查未改，重构未闭环。
- **session.md 三副本 `dsettings.json` 残留**：L79 仍写 `dsettings.json → error_tracking_enabled`。

#### 不完全认可的项目
- **drec/dstat 无 ideas 触点**：属于产品完整性增强而非契约级缺口。P2，可合后修。

### 判断结论
P1 两项（键名 + completeness.md）必须在合并前修。涉及 4 个键名 × ~12 处引用替换 + 1 个 completeness.md 重写，全部机械性操作预计 ~60 行改动。

### 下一步
1. 修 4 个键名（模板/实例/迁移器/消费者 `context_monitor.py`）+ 3 个 rules 副本 `dsettings.json` 引用
2. 重写 completeness.md 为 8 类型模型
3. 全量 pytest 377 绿后 push

### 本次踩坑/经验
- [数据缺口] #31 D3 的 9 个键名中的 4 个上下文感知键名（recording_retention_days + 3 个 ctxmon_*）在前两轮修复被漏掉——只关注了"task_archive/recording_archive/review_limit"这一组映射，没交叉引用 Issue body 原文逐条核对 → 以后做键名映射类修复时，先把 issue 的完整映射表打印出来逐条打勾
- [读层现象] completeness.md 四层模型是 Issue #24 早期草案的产物，最终锁定结论要求在 doc-types.md 中表达，但 completeness.md 未随之上移 → 跨 issue 依赖的文档链需要显式校验，不能依赖"写了 doc-types.md 自然就覆盖到了"
