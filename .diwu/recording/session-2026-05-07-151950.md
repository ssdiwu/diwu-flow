## Session 2026-05-07 15:19:50

### stop_decision.py 退出码修复 + v0.0.12 补发
**实施内容**: stop_decision.py:692 将 `sys.exit(0 if should_continue else 1)` 改为 `sys.exit(0)`——无 InProgress 任务时正常结束不再显示红色 error 提示；真正的错误场景（行650 Stop事件缺session_id）仍保留 exit(1)
**验收验证**: - [x] 源码 hooks/scripts/stop_decision.py 已同步修改（Read 验证） - [x] /reload-plugins 后新 session 加载新代码
**提交**: commit (pending)

### 下一步
- v0.0.12 补发版（含此 fix）

### 本次踩坑/经验
- **[路由护栏契约]** hook 框架将 exit(1) 统一渲染为 "non-blocking error" → stop_decision 用 exit 区分"继续/停止"语义导致每次正常结束都显示红色错误 → 正确做法：状态判断结果通过 stdout JSON 输出，退出码统一用 0
- **[验证误读]** 插件缓存文件和源码仓库需同步修改，否则 /reload-plugins 前旧代码仍在运行
