## Session 2026-05-01 20:58:41
### Task#58: task_entry_guard 硬阻止退出码修正 exit(1)→exit(2) → Done
**实施内容**: PreToolUse hook 的硬阻止分支（dloop non-owner guard + plan hard block）从 sys.exit(1) 改为 sys.exit(2)。根据 Claude Code 官方 hooks 文档：exit(0)=允许 / exit(1)=非阻塞错误(继续执行) / exit(2)=拒绝工具调用。同步更新测试断言 test_plan_dtask_guards.py L229 从 returncode==1 改为 ==2。
**验收验证**: 七分支验证全通过 — dloop 6 分支(0/0/2/2/0/0) + plan hard block(2)；258/258 pytest 全绿；测试断言已对齐官方 PreToolUse 语义
**提交**: pending
### 下一步: 无待办
### 本次踩坑/经验
- [路由护栏契约] Hook 阻止机制是平台契约不是实现细节。exit(1) vs exit(2) 在 Claude Code PreToolUse 中有完全不同的语义——前者只是"报错但继续"，后者才是"真正拒绝"。之前四轮迭代都在调判定逻辑，但连"怎么阻止"这个基础机制都用错了，导致所有 hard block 实际上都是 soft warn
- [验证误读] 脚本级 returncode 断言 ≠ hook 运行时行为验证。pytest 测试只证明"脚本返回了什么码"，不证明"Claude Code 是否真的拦截了工具调用"。测试断言必须与平台文档的退出码语义对齐，否则绿色测试会掩盖假阳性
