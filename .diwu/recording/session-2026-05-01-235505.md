## Session 2026-05-01 23:55:05
### Task#46: dinit.py scan-repo file_count 字符串化 TypeError → Done
**实施内容**: 确认代码已正确（len() 返回 int，min(int, 9999) 返回 int），补充 3 条回归测试锁定行为
**验收验证**:
- [x] file_count_estimate 始终为 int 类型（155+ 文件目录测试）
- [x] 9999 封顶保持整数
- [x] 权限不足目录返回 0 且不崩溃
- [x] pytest 16/16 dinit tests passed（+3 新增）
**提交**: commit fc83cbd

### Task#57: 归档 marker 机制：/darc 合法缩减 dtask.json 时 guard 放行 → Done
**实施内容**:
- task_entry_guard.py 新增 _ARCHIVING_MARKER + _is_archiving_in_progress()
- main() 插入 marker 检查：仅对 dtask.json 写入放行，其他文件正常 guard
- darc SKILL.md 新增步骤 0（touch marker）和 4.5（rm marker）
- 6 条测试全覆盖四类场景
**验收验证**:
- [x] marker 存在 + 写 dtask.json → exit(0) 放行
- [x] marker 存在 + 写其他文件 → 正常 guard 链路不变
- [x] 无 marker → 行为与改动前完全一致
- [x] marker 删除后 guard 恢复正常拦截
- [x] marker 仅豁免 dtask.json，不豁免 .diwu/ 其他文件
- [x] python3 -m py_compile 通过
- [x] pytest 273 passed（264 + 3 dinit + 6 archiving marker）
**提交**: commit fc83cbd

### 下一步:
- 完成 v0.0.7 发版（public push）
- 可选：继续推进其他 InDraft 任务

### 本次踩坑/经验
- [路由护栏契约] baa064d 误清 dtask.json 的根因是普通 session 中直接覆盖了 status 真相源，违反只有 /darc 能做归档的规则 → 已通过 Task#57 的 marker 机制从 guard 层面固化此约束（marker 不存在时任何大幅缩减 dtask.json 都会被拦截）
- [读层现象] scan-repo 的 file_count bug 在 Task#48 批量修复期间已被顺带修复，但缺少回归测试 → 补充后锁定为 267→270 的测试增量
