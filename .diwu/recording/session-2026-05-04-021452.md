## Session 2026-05-04 02:14:52

### 修复：dadr→ddoc 合并回归 + 状态对齐 + 测试加固

**修复项**:

#### [P1] ddoc_adr.py README 索引覆盖 bug
- **根因**: `_parse_readme_index()` L82 用 `re.match(r"(\d+)", "ADR-001")`，`A` 非数字 → 返回 None → 整行跳过
- **修复**: `re.match` → `re.search`
- **验证**: 创建 2 个 ADR 后 README 保留 ADR-001 + ADR-002（修复前只剩新建的那个）

#### [P2-1] ddoc_adr.py README 重建状态失真
- **根因**: README 缺失重建路径 L175 硬编码 `"status": "Proposed"`，丢失已有 ADR 的真实状态
- **修复**: 新增 `_extract_status()` 函数，从 ADR 文件正文解析 `**Status**: xxx` 行
- **验证**: ADR-001 设为 Accepted 后删除 README 再 create 第三个 ADR → 重建索引中 ADR-001=Accepted, ADR-002/003=Proposed

#### [P2-2] Task#72-74 dtask.json 状态与 commit 不一致
- **根因**: 4344bd7 commit message 写 Done 但 dtask.json 未执行 release 迁移
- **修复**: 正规迁移链 InDraft→InSpec→InProgress→Done（每个 task-id 单独走完）
- **当前状态**: Task#72-76 全部 Done

#### [P2-3] bdae713 提交含 dloop 残留运行态
- **现状**: dloop 已为 None（Stop hook 已清理），但 pending_recording 标记仍存在
- **本次 closeout 将清除 pending_recording**

#### [次 1] Agent model 测试未锁精确值
- **修复**: CORE_AGENT_MODEL_EXPECTATIONS 中 explorer/implementer 从 allowed_models 改为 required_model（精确值匹配）
- **验证**: explorer 必须 haiku、implementer 必须 sonnet；改值即失败

#### [次 2] README Version 段 v0.0.10
- **修复**: 更新为 v0.0.11 + 重写摘要

**验收验证**:
- [x] pytest tests/ -q 317 passed
- [x] pytest tests/level1/test_agents_config.py -k model 3/3 (精确值)
- [x] pytest tests/level2_scripts/test_ddoc_adr.py -v 9/9
- [x] P1 复现: 2 ADR 创建后 README 索引完整保留
- [x] P2-1 复现: Accepted 状态在 README 重建时正确保留

### 下一步: 无待办任务（本轮 dloop 已完成）

### 本次踩坑/经验
- [正则陷阱] `re.match` 从字符串开头匹配，`ADR-001` 中数字不在开头 → 用 `re.search` 替代
- [原子性铁律] status 变更必须与代码变更同一 commit；仅修改 dtask.json 不算完成 closeout
- [测试设计] 允许范围检查 vs 精确值断言：前者防回退但不防误标，后者锁住关键约束
