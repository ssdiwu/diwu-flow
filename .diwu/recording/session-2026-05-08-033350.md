## Session 2026-05-08 03:33:50

### #7 Commands / Skills 分层重整讨论 — 四个方向收敛

**讨论议题**：[Design] Commands / Skills 分层重整与新增删减原则

**实施内容**：

- A. ddoc 正向/逆向模式设计：读取三个样本项目 .doc/（AIBC/Curio/diwu-flow）+ 行业实践（arc42/eriklieben/DocuWriter），提取通用文档矩阵。排除 ADR/RFC/Spike/事故复盘/审查报告（被 dtask/issue 代替），合并部署运维+Runbook。最终收敛为 9 个设计文档 + 1 个导航索引。核心定义：`.doc/` 是唯一的 设计文档真相源目录，文档先行。
- B. drun 双入口模型：drun 从「dtask runner」升级为 Task Contract Runner。direct run 三条件：目标（刚性）、完成标准（刚性）、改动范围（可松，需边界约束）。缺任何一个 → /dtask 先定义。升级条件：改动范围超出预期/依赖其他任务/architect 命中/需跨 session。
- C. 持久化策略：所有执行都必须 drec 记录。大变更（>200行/多模块/契约变更/需L1-L2证据）→ 升级到 dtask + drec 双写；小变更 → drec only；无代码变更 → drec（记录对话结论）。删除 none 策略。
- D. 新增/删除/溶解原则：定义 command/skill 生命周期规则。新增 command 需满足三条件（独立入口+方法论+触发场景）。审计结果：9 对 skill/command 一一对应，dvfy 应删除，/dstop 无 skill 合理（薄壳终止器）。

**验收验证**：
- [x] #7 issue comment 已更新 5 条（issue 共 6 条 comment）

**提交**：无代码变更，纯设计讨论

### 下一步

- #7 剩余待讨论：是否需要 skills/README.md / commands/README.md；是否整合最终设计文档
- 继续推进其他 issue（#1-#6, #8）

### 本次踩坑/经验

- [读层现象] dcorr 融合进 #5 handoff 的提议 → 分析后发现 dcorr 与 handoff 是正交维度（元认知 vs agent 流转）→ 误判为应该融合 → dcorr 保留独立定位，但需对齐交接锚点和跨域回退两处重叠
- [数据缺口] direct run 三条件初始设计过松（一句话目标即可）→ 用户指出目标/完成标准必须刚性、改动范围才能松 → 修正为阶梯式三条件，与大规模判定对齐
