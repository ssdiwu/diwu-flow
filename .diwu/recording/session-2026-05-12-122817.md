## Session 2026-05-12 12:28:17

### Task: #54 框架讨论收口 → 正文回写 + Phase 0 就绪

**实施内容**:
- 逐条回应 RexYoung000 外部审视评论（7 条建议 + 风险分析 + 落地路径）
- 收敛 drun × ddoc × drec 协同边界（.doc/ 写入原则修正、dtask 归档→Issue）
- 锁定 dfeat 八板块完整字段定义（TOML schema + 中文注释 + P0/P1/占位标注）
- 扩展状态机：新增 InDefine（收敛中）+ InHold（暂存），复用 In+名词规律
- 精简 dtask 状态机：6 态→4 态（去 InDraft/InSpec 定义态，Cancelled 缩为 task 级）
- 锁定 Phase 0 MVP 范围（P0-1~P0-4：dfeat schema / dstat --feat / .diwu/ 新结构 / dinit 迁移）
- 收敛远程映射时机（Issue 过 gate 后创建、关键节点更新、PR 对齐）
- 确认标签映射（新增「进行中」「待决策」两个标签）
- 确认 dinit 自动迁移模式（init/migrate 无感知，用户只敲 /dinit）
- **A 阶段**：4 处正文回写（§6.2 字段定义 / §10.5 协同边界 / §14 Phase 0 MVP / §9.4 远程映射）
- **B 阶段**：P0 最小不可争议版 dfeat.toml 字段收缩清单（16 字段必实现 + 3 示例 + 注释规范）
- **C 阶段**：完整实施映射表（14 新增 + 6 修改 + 3 deprecated + 52 不动）

**验收验证**:
- [x] #54 正文 §6.2/§10.5/§9.4/§14 均含"实施依据：评论 #N"引用，正文可独立成立
- [x] #54 标签从「讨论中」→「可实施」
- [x] 12 条评论覆盖：框架定义(3) + 外部审视(1) + 回应(1) + 字段定义(1) + MVP(1) + 远程映射(1) + 迁移(1) + P0收缩(1) + 映射表(1)
- [x] P0 范围精确到字段级（16 必实现字段 + 23 P1/占位）

**提交**: 无本地 commit（所有工作在 GitHub issue #54 上完成）

### 下一步
- 开 v0.2.0 独立 PR，按 C1 实施映射表的 7 步顺序落地 Phase 0
- 创建新标签 `进行中` 和 `待决策`（gh label create）

### 本次踩坑/经验
- [分层未拆清] .doc/ 写入模型最初设计为"每个 feat 建 .doc/ 子目录" → 被 507 纠正：.doc/ 是真相源不应被 feat 归档污染，改为增量 patch 受影响的现有文件
- [路由护栏契约] gate-lite 提议最初倾向采纳 → 被 507 纠正：守门不能偷巧，小改动应在入口层分流不进 feature 体系
- [读层现象] 状态命名 InRefining/InStayed 混用 ing/ed 时态 → 被 507 纠正：统一为 In+名词规律（InDefine / InHold）
- [验证误读] 多次编辑 #54 正文时替换锚点文本未命中（全角/半角冒号、空格差异）→ 教训：必须用行号定位或 raw bytes 对比确认匹配文本
