## Session 2026-05-11 01:29:15
### Task: Issue #47 Windows git 性能清除 + context_monitor 落位修复 → Done

**实施内容**:
- Issue #47 第三阶段完成：新增 `hooks/scripts/_fs_snapshot.py`，用 `.git/HEAD`、`.git/index`、`logs/HEAD` 与文件系统扫描替代 git CLI
- `hooks/scripts/stop_decision.py` 完全去 git：移除 slow path 中 `git status/diff/ls-files` 依赖，保留 fast-path/slow-path 分流
- `hooks/scripts/pre_compact.py` 完全去 git：删除 `git diff --stat` / `git diff --cached --stat`，仅写 `[auto-compact]` 标记
- `scripts/dstat.py` 完全去 git：分支、最近提交、工作区状态、deep 模式详情全部改为 `_fs_snapshot` 直读 `.git` 与文件系统
- `hooks/scripts/context_monitor.py` 修复 cwd 依赖：读取 `dsettings.toml` 与写入 `.context_monitor_cache_{sid}.json` 统一基于事件 `cwd`，不再依赖进程 cwd
- 清理误落位运行态目录 `hooks/scripts/.diwu/`
- 补充 `_fs_snapshot.py` 直接测试（非 git / clean / modified / untracked / .diwu dirty / packed-refs fallback / index version unsupported / project snapshot）
- 发版前信息校验：`CHANGELOG.md` 已追加 Issue #47，`README.md` 测试数更新到 389，Issue #47 已回写最终状态并关闭
- 额外一并纳入工作区的非 #47 变更：`.claude/CLAUDE.md` GitHub 标签规则；`scripts/dinit.py` + `tests/level2_scripts/test_dinit.py` ideas archived 迁移逻辑

**验收验证**:
- [x] `python3 -m pytest tests/ -q` → 389 passed
- [x] `python3 -m pytest tests/level2/test_fs_snapshot.py -q` → 9 passed
- [x] `python3 -m pytest tests/level2/test_context_monitor_dict.py tests/level2/test_stop_decision.py tests/level2_scripts/test_dstat.py -q` → 37 passed
- [x] `stop_decision.py` 零 git 子进程，本地耗时约 45ms
- [x] `dstat.py` 零 git 子进程，本地耗时约 48ms
- [x] Issue #47 最新状态已评论并关闭

**提交**:
- 待本次 /drec 原子 commit

### 下一步
如需继续收口，可单独评估 `_fs_snapshot.py` 对 packed object subject 的能力边界是否值得增强。

### 本次踩坑/经验

- [分层未拆清] 最初只盯 `stop_decision.py` 做局部优化 → 后续排查发现 `pre_compact.py` 和 `dstat.py` 也有同类 git CLI 依赖。正确做法：性能问题先做全量调用面扫描，再决定修复边界。
- [环境漂移] `context_monitor.py` 使用相对路径写缓存，某次在错误 cwd 下把运行态文件写到 `hooks/scripts/.diwu/` → 表面看像脏源码，实际是落位 bug。正确做法：hook 运行态文件一律基于事件 `cwd` / 项目根拼绝对路径。
- [验证误读] `_fs_snapshot.py` 已通过上层脚本间接覆盖，但最开始没有直接测试 → 直接操作 `.git/index` / `logs/HEAD` / loose object` 的核心模块风险被低估。正确做法：共享底座模块必须有自己的 L2 直接测试，不只依赖下游间接覆盖。
