## Session 2026-05-10 16:16:48

### Task: Issue 关联网络建立 + 命令执行型 Skill 审查讨论 → InProgress
**实施内容**:
- **GitHub Sub-issue API 调研**: `gh` CLI 不支持 parent 字段；REST API 也不直接支持；GraphQL `addSubIssue` mutation 可用（`UpdateIssueInput` 不接受 `parentIssueId` 参数）
- **#35 → #26 Parent 关系建立**: 通过 GraphQL `addSubIssue` mutation 成功设置 #35 (Skill SOP 收缩) 的 parent 为 #26 (Skill 体系改进)
- **Blocked By API 发现**: GraphQL 有 `addBlockedBy` / `removeBlockedBy` mutation 可用于依赖关系
- **相关 Issues 状态确认**: #24(ddoc重构,P1,可实施) / #26(Skill体系改进,P1,可实施) / #29(didea重塑,P1,可实施) / #37(dinit同步清单,P2,可实施) / #35(Skill SOP收缩,P2,讨论中)

**验收验证**:
- [x] #35 parent → #26 关系已通过 GraphQL API 确认生效
- [x] `addSubIssue` / `addBlockedBy` API 能力确认

**提交**: 本次 /drec 执行中

### 下一步:
1. **#35 blocked by 关系补充**: #35 被 #29 阻塞（didea 重塑是 didea 启发式重构的前置）→ 用 `addBlockedBy` 建立
2. **#24 与 #35 关联**: #24 (ddoc 重构) 是 #35 P1 阶段的上游 → 确认是否设 parent 或 blocked by
3. **命令执行型 Skill 审查**: 507 指出"命令执行型(B类)也要看看，SOP 画风偏重不代表不存在问题"→ 需审查 drun/dloop/drec 三个 B 类 skill
4. **#25 E5-E6 继续**: dpth Active 与 L-Skill 集成规格 + 成本性能考量
5. **#27 实施**: drelease.sh dry-run + pytest 门控

### 本次踩坑/经验

- [API探索] **gh CLI 不支持 sub-issue/parent 功能**：`gh issue edit --help` 无 parent 选项；`gh issue view --json` 无 parent 字段；但 GitHub GraphQL API 支持 `addSubIssue` mutation。教训：**GitHub 新功能（sub-issues 2024年推出）往往先在 GraphQL API 上线，CLI 工具跟进有延迟——需要时直接查 GraphQL schema**
- [API探索] **`UpdateIssueInput` 不接受 `parentIssueId`**：直觉以为 updateIssue 可以设置 parent，实际只能通过专门的 `addSubIssue` mutation 操作父子关系。教训：**操作关系型字段前先查对应 mutation 的名字（addXxx/removeXxx），不要假设通用 update 能覆盖**
