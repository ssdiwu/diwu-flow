## Session 2026-05-11 16:16:18

### Task: Issue #51 归档 TOML 迁移 + dinit 三层架构重构 → Done

**实施内容**:
- `commands/dinit.md` 削薄为 22 行纯触发器（原 94 行 SOP），删除子命令表/Step 编号/refresh 参数泄露
- 新建 `skills/dinit/SKILL.md`（85 行），启发式风格：判断视角/红旗信号/陷阱提示，无 Step SOP
- `scripts/dinit.py` 新增 `cmd_run()` 自动编排入口 + 注册 `run` 子命令
- `scripts/dinit.py` `cmd_migrate_legacy()` 追加归档文件 JSON→TOML 迁移分支（task_archive_*.json + .last_archive_summary.json）
- `scripts/drec_archive.py` 输出改为 TOML，新增 `_load_archive()` 兼容 JSON/TOML 读取
- `.claude-plugin/plugin.json` skills 列表注册新 skill
- 测试适配：`test_task_166_dinit_diwu_paths.py` 改为检查 SKILL.md 而非 command.md

**验收验证**:
- [x] L3 全量回归 381/381 PASS
- [x] L2 归档专项 11/11 PASS
- [x] cmd_run() 入口正常返回 refresh 模式编排结果
- [x] plugin.json frontmatter 合法性通过
- [x] Issue #51 已关闭并回写完整方案 + 实施记录

**提交**: 待 commit

### 下一步:
- 清理 .diwu/ 下残留的旧 context_monitor_cache 文件（Issue #49 遗留）

### 本次踩坑/经验

- [分层未拆清] commands/dinit.md 承担了触发器+SOP+脚本说明书三重角色 → 根因是缺少 skills/dinit/ 这一层 → 正确做法是三层分离：Command(触发器)→Skill(启发式文档)→脚本(实现)
- [路由护栏契约] 测试断言绑定到 command.md 的具体措辞（Step 0/1/2）→ 重构后 command 削薄导致测试失败 → 应将 AI 步骤断言指向 Skill 文件而非 Command 文件
- [读层现象] 用户反馈"不应暴露命令给用户" → 检查发现 argument-hint 含 refresh、子命令表格直接展示、快捷用法提示参数 → 这些都会流入 AI→用户的对话 → 解决方案是 Command 只留行为契约，细节下沉到 Skill 并标注"AI 内部参考"
