## Session 2026-05-05 14:05:47

### Task#?: 三迭代实施审查 + 文档补全 + 回归验证 → Done

**实施内容**:
- 审查迭代 A（Hook 可观测性/阻断策略）、迭代 B（Session ID scoped 存储）、迭代 C（卸载健壮性）三块代码实施完整性
- 修复 `test_task_168_no_hardcoded_paths` 测试白名单遗漏（run_hook.py 三元表达式 else os.getcwd()）
- 文档补全 6 处缺口：README 安装表+scripts 列表、file-layout.md scoped 文件条目、constraints.md 原子写入实例、架构规范 Hook 表头+包装层语义+scoped 路径、工程规范卸载健壮性条目
- CHANGELOG 补充三迭代 Infrastructure 条目，后按用户要求从 v0.0.11 迁移至 v0.0.12
- rules/file-layout.md + constraints.md 变更同步至 `.claude/rules/` 和 `assets/dinit/assets/rules/` 两处副本

**验收验证**:
- [x] 全量测试 325 passed, 0 failed (pytest tests/ -q)
- [x] verifier 子代理独立验证：代码质量 6/6 PASS、集成路径 3/3 PASS、边界条件 3/3 PASS、Stub 扫描 CLEAN
- [x] explorer 子代理文档审查：6 处缺口全部补齐
- [x] Rules 三副本同步完成（file-layout + constraints）

**提交**: 待本 session commit

### 下一步: v0.0.12 发版准备

### 本次踩坑/经验

- [验证误读] test_task_168_no_hardcoded_paths 白名单只覆盖了 `or os.getcwd()` 和 `globals()...os.getcwd()` 两种模式 → 遗漏 run_hook.py 的三元表达式 `else os.getcwd()` fallback → 补充 GETCWD_TERNARY_ELSE_PATTERN 正则后通过。教训：白名单正则需覆盖同一语义的所有语法变体（or / ternary else / 默认参数）
