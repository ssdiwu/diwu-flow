[[tasks]]
id = 135
title = "dtask.json / dtask-state.json → TOML 迁移"
description = "将 dtask.json 和 dtask-state.json 从 JSON 迁移到 TOML，统一全项目数据格式。dsettings.toml 已先行迁移，这是遗留架构项。涉及 ~55 个文件（scripts/hooks/tests/rules/docs/templates）。\n\nTOML Schema 见 https://github.com/ssdiwu/diwu-flow-dev/issues/48"
acceptance = [
    "Given dtask.json/dtask-state.json 已改为 .toml 后缀  When 执行 pytest tests/ -q  Then 全量回归通过，无退化",
    "Given dtask.toml 和 dtask-state.toml 存在 When 用 tomllib.load() 读取 Then 解析成功无异常",
    "Given 所有 Python 脚本和 Hook 脚本 When 读取状态文件 Then 使用 TOML 格式读取，函数名和路径常量统一更新",
    "Given rules/skills/commands/.doc/ 中所有引用 When 全局搜索 dtask.json/dtask-state.json Then 全部替换为 dtask.toml/dtask-state.toml",
    "Given task.json.template When 迁移为 TOML 后 Then dinit.py 模板引用和 manifest.json 已更新",
]
steps = [
    "Layer 1: 基础设施 — common.py 路径常量 + _shared.py load_toml_fallback + dtask_state.py 全量改写",
    "Layer 2: Python 消费者 — dtask_transition.py dloop.py dstat.py drec_archive.py dinit.py",
    "Layer 3: Hook 脚本 — 7 个 hook 脚本路径和读写函数切换",
    "Layer 4: 测试夹具 — ~17 个测试文件 json.dumps → tomli_w.dumps",
    "Layer 5: 文档/规则 — ~20 个文件 .json → .toml 字符串替换",
    "Layer 6: 模板 — task.json.template → task.toml.template + manifest 更新",
]
files_modified = []
category = "refactor"
blocked_by = []
status = "Done"

[[tasks]]
id = 136
title = "重写 drec_write.py 去掉 draft 中间态"
description = "删除 _read_draft/_write_recording/_remove_draft/DRAFT_FILE，新增 _find_latest_recording，run() 从 9 步简化为 6 步。AI 直接写 recording 文件，脚本只做归档+commit。"
acceptance = [
    "Given AI 已写入 .diwu/recording/session-{ts}.md  When 调 drec_write.py run  Then 检测到该文件并正常 commit",
    "Given 无 recording 文件  When 调 drec_write.py run  Then 返回 ok=false status=failed 含 recovery_hint",
    "Given 有 pending_recording 且在时间窗内  When 调 drec_write.py run  Then amend 成功",
    "Given commit 失败(pre-commit hook)  When 调 drec_write.py run  Then partial_success + recording/pending 均保留",
]
steps = [
    "删除 DRAFT_FILE 常量、_read_draft()、_write_recording()调用、_remove_draft()",
    "新增 _find_latest_recording() 扫描 recording/ 取最新 mtime 文件",
    "重写 run(): Step1 改为找 recording → 删写 recording 步 → 删删 draft 步",
]
category = "infra"
status = "Done"

[[tasks]]
id = 137
title = "更新 drec_write 测试适配新流程"
description = "_write_draft() → _write_recording() 直接写到 recording/ 目录，删 _clean_draft()，TestMissingDraft 改为无 recording 场景。"
acceptance = [
    "Given 写了 recording 文件+工作区变更  When 调 drec_write.py  Then committed + recording_path 正确",
    "Given 无 recording 文件  When 调用  Then expect_ok=False status=failed",
    "Given pre-commit hook 拒绝  When 调用  Then partial_success + 三保留不变量",
    "Given 归档脚本缺失  When 调用  Then commit 仍成功不阻塞",
]
steps = [
    "_write_draft(root) → _write_recording(root, ts) 写入 recording/ 目录",
    "删除 _clean_draft() 调用",
    "TestMissingDraft: 不写任何文件 → expect_ok=False",
]
category = "testing"
blocked_by = [
    136,
]
status = "Done"

[[tasks]]
id = 138
title = "同步 SKILL.md R12 + commands/drec.md + Issue comment"
description = "R12 调用契约去掉 draft 引用改为直接写 recording，commands/drec.md 写入目标改 recording/，gh issue #53 comment 回写 v0.1.3 补丁说明。"
acceptance = [
    "Given skills/drec/SKILL.md R12  When 阅读  Then 无 .drec_draft.md 引用，AI 职责为直接写 recording",
    "Given commands/drec.md  When 阅读  Then 步骤2 目标为 recording/ 目录非 .drec_draft.md",
    "Given Issue #53  When 读 comment  Then 包含 draft 去除说明和 v0.1.3 补丁标注",
]
steps = [
    "SKILL.md R12: AI职责第2条→写recording, 脚本职责→检测recording/删写recording/删删draft",
    "commands/drec.md: 步骤2目标路径+时间戳说明更新",
    "gh issue comment #53: 补丁说明",
]
category = "文档"
blocked_by = [
    137,
]
status = "Done"
