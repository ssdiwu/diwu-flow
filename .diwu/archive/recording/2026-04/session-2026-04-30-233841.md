## Session 2026-04-30 23:38:41
### Task#31: 清理 dinit 用户话术与索引命名 → Done
**实施内容**:
- `assets/dinit/assets/claude-md.template`：将索引章节标题从 `Skill 与参考文件索引` 统一为 `Skill 索引`，补齐 Task#31 最后一处未满足的验收项
- `hooks/scripts/stop_decision.py`：将“缺失 `session_id`”路径改为显式 `allow_stop` 并提前退出，避免落回 default mode 造成 `invalid_runtime_state`
- `hooks/scripts/task_entry_guard.py`：为 plan hard-block 增加当前项目 marker 前置条件，避免被全局 `~/.claude/plans` 残留误伤，恢复全量测试可用性
- `skills/drun/SKILL.md`、`commands/drun.md`：补强 `/drun` 状态流转约束，明确开始必须 `claim`，结束必须用 `dtask_transition.py release` 将任务显式切到 `Done` 或 `InReview`

**验收验证**:
- [x] `v2` 内部世代话术零命中：`rg -n "v2 格式|v2 升级|v2 精简" commands/dinit.md assets/dinit/assets/claude-md*.template` 无输出 (L3)
- [x] 三份模板索引标题已统一：`rg -n "Skill 索引|Skill 与参考文件索引" assets/dinit/assets/claude-md*.template` 仅命中 `Skill 索引` 变体 (L1)
- [x] `dinit` 定向回归通过：`pytest tests/level2_scripts/test_dinit.py -q` → `13 passed` (L3)
- [x] dloop / stop hook 定向回归通过：`pytest tests/level2/test_stop_decision.py tests/level2/test_dloop.py tests/level2_scripts/test_dloop_crud.py -q` → `45 passed` (L3)
- [x] Python 语法检查通过：`python3 -m py_compile hooks/scripts/stop_decision.py hooks/scripts/task_entry_guard.py hooks/scripts/task_completed.py scripts/dloop.py` (L3)
- [x] 全量回归通过：`pytest tests -q` → `226 passed` (L3)

### 下一步: 继续收口 Task#33-39 的 dloop / plan-guard WIP，或按需提交当前工作区
### 本次踩坑/经验

- [状态与真相源脱节] 先前 commit/recording 已写 “Task#31 completed”，但当前 `dtask` 验收里仍有一处标题统一未真正落地 → 误把历史结论当成现场真值 → 正确做法：收尾前必须回到当前 `dtask.acceptance` 和工作树逐条对照，而不是只看旧 recording/commit message
- [全局状态泄漏] `task_entry_guard.py` 直接扫描真实 home 下的 `~/.claude/plans`，导致其他项目残留 plan 也能把本项目测试打成 hard block → 误把“用户全局态”当成“当前项目上下文” → 正确做法：所有 guard 类逻辑都要先有当前项目 marker / provenance，再读取全局目录
