## Session 2026-04-30 19:00:38

### Task#23: 修复 dloop stale-state 残留 → Done

**实施内容**:
- `scripts/dloop_state.py`：三分类 helper（active_or_recoverable / terminal_stale / invalid_state），含 `classify(cwd, dtask_data, settings)` 和 `cleanup_state(cwd)`
- `scripts/dloop.py` cmd_start()：stale 检测 + 清理后继续启动（不提前返回）+ `stale_cleanup_reason` 注入 message
- `scripts/dloop.py` cmd_status()：stale 检测 → stale_cleaned / invalid_state_file 两条路径
- `_state_path(cwd)` 单一路径 helper，消除 start/status 路径构造差异
- `tests/level2_scripts/test_dloop_crud.py` 新增 7 个 stale 测试 + 2 个路径完整性测试
- `commands/dloop.md` + `skills/dloop/SKILL.md` 文档补充 stale-state auto-clean 说明
- `hooks/scripts/stop_decision.py` max_tasks 默认值从 10→0，notify() 增加 DIWU_SILENT 静默开关

**验收验证**:
- [x] terminal_stale(status) → stale_cleaned + 文件删除 ([subprocess CLI-first])
- [x] terminal_stale(start) → 清理后 started + message 含清理提示 ([subprocess])
- [x] active_or_recoverable → running/already_running 且文件不删 ([subprocess])
- [x] invalid_state → invalid_state_file 且文件不删 ([subprocess])
- [x] path integrity: status 读正确路径 + start→status 一致性 ([subprocess])

**验收**: 全量 205 passed

---

### Task#24: 锁定 dloop.py 子进程真值来源 → Done

**实施内容**:
- `_state_path(cwd)` 单一路径 helper 统一 start/status 路径构造
- `TestDloopPathIntegrity` 2 个测试：status 读正确路径证明无 .diwu/.diwu bug；start→status 一致性证明路径共享
- 测试均需 dtask.json 含可执行任务（classify 会传 dtask_data 检查无可执行任务条件）

**验收验证**:
- [x] status_running_reads_correct_state_path: 只在 <cwd>/.diwu/ 创建 state，无 .diwu/.diwu 错误路径 ([subprocess])
- [x] start_and_status_use_same_state_path: start 创建的文件必须被 status 读到 ([subprocess])
- [x] 全量 205 passed ([pytest regression])

**提交**: 代码变更未 commit（dloop 循环中）

### 下一步: Task#25+（如存在）或 dloop 循环结束

### 本次踩坑/经验

- [验证误读] test_start_conflict 和 test_status_running 回归失败 → 传入 dtask_data 后 classify() 的条件 2（无可执行任务）开始生效，但旧测试未提供 dtask.json → 误以为是 stale 检测逻辑错误，实际是测试缺少前置数据 → 为所有 active=true 的测试补充 dtask.json 含可执行任务
- [分层未拆清] test_start_terminal_stale_clean_and_continue 期望 status=="started" 但实际返回 stale_cleaned → start 的 stale 检测原逻辑是提前返回 stale_cleaned → 修正为清理后继续启动（不提前返回），但需记住 start 语义是"清理+继续"而 status 是"清理+告知"
