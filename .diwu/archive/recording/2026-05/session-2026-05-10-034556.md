## Session 2026-05-10 03:45:56

### Task: #26 Skill 体系改进 — 触发机制设计深入 → InProgress
**实施内容**:
- 三方开源项目并发探索（superpowers 184k+ stars / ui-ux-pro-max / planning-with-files）
- 方案演进：A（增量）→ B（元 skill 分发）→ C/Cv2（hook 路由注入）→ B-only（纯静态分发）
- didea 失效诊断：triggers 召回率实测 10%（12 条真实对话仅命中 2 次）
- `/diwu` 元 skill 完整草案设计（Priority Layering / SHOULD 规则 / 输出格式）
- Hooks 全量审计 → #28 新建（P1: context_monitor 无 session 隔离 / drift_detect stdin 冲突）
- #29 新建：didea 存储模型重塑（从被动仓库到主动感知层，四方向 A/B/C/D 待选）

**验收验证**:
- [x] triggers 覆盖率测试通过（数据驱动决策：10% 召回率证明需元 skill）
- [x] 三方项目对比报告完成并同步到 #26
- [x] Cv2 方案否决理由文档化（多窗口 session 隔离循环依赖）
- [x] Issue 关系图确定（#28 并行 / #29 依赖 #26 / #24 顺延 / #25 暂挂）

**提交**: 待 commit（本次 /drec 执行中）

### 下一步:
1. #26 Phase 1 实施：删除 type 字段 + skills/README 同步
2. #26 Phase 2 核心：`/diwu` SKILL.md 正式化 + 11 个 description CSO 改写
3. #28 P1 修复：context_monitor 缓存加 session_id + drift_detect stdin 防护
4. #29 讨论方向选定后实施 didea 重塑

### 本次踩坑/经验

- [读层现象] triggers 数组看似结构化完善，实际对自然对话召回率仅 10% → 根因：triggers 捕捉指令式表达（"先记下来"），真实对话是发散式表达（"聊聊这个"、"我觉得可以这样"）。正确做法：用语义意图检测替代关键词匹配（/diwu 元 skill L0 层）
- [分层未拆清] Cv2 hook 注入方案被否决的根因与 dloop cron 迁移是同一个问题（session owner 不可靠）→ 但表现形式不同（Cv2 是"per-session 上下文注入"，dloop 是"任务归属判断"）。教训：同一根因会在不同方案中以不同面具出现，需要识别本质而非处理表象
- [验证误读] superpowers 的 1% 规则不能直接搬用到 diwu-flow → 误判为通用最佳实践。实际差异：superpowers 误触代价低（多读 frontmatter），diwu-flow 误触代价高（可能触发完整方法论执行干扰工作流）。修正：引入 SHOULD 档位替代 MUST，保留 AI 最终裁决权
- [路由护栏契约] didea 作为 L0 入口在本次 session 完全沉默（7+ 次应触发零命中）→ 暴露的不是单个 skill 问题而是整个触发模型假设缺陷（"用户会主动用 command/skill" vs "用户只是自然聊天"）。这直接推动了 /diwu 元 skill 从"可选增强"升级为"核心基础设施"
