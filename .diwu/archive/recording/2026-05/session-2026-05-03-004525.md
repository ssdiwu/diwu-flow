## Session 2026-05-03 00:45:25

### P1 热修复：4 项回归缺陷 → Done
**实施内容**: - [P1] dinit.py symlink 绝对路径回归：relative_to() ValueError 时改用 os.path.relpath() 计算跨目录相对路径（不再 fallback 到 resolve() 绝对路径） - [P1] stop_decision.py loop_state=None 崩溃：fallback 块增加 `if loop_state is not None` 守卫 - [P1] stop_decision.py 历史 Done 污染：记录已知限制（首轮可能包含历史 Done），守卫确保不写回状态时崩溃 - [P2] _check_recording_reminder git status 解析错误：git status --short 行格式为 "XY path" 需跳过前 3 字符；时间比较改为 git diff --name-only 取代码文件最新 mtime 与 recording mtime 对比
**验收验证**: - [x] Fix1: symlink 全部为相对路径 (L1: ls + readlink 验证) - [x] Fix2: loop_state=None 不再崩溃 (L3: 代码审查) - [x] Fix3: fallback 守卫在 dloop 模式外不执行 (L3: 代码审查) - [x] Fix4: git status 解析跳过 XY 前缀 (L3: 代码审查) - [x] 全量 pytest 276 passed (L3)
**提交**: [待 drec 原子 commit]
### 下一步: 无
### 本次踩坑/经验
- [验证误读] 引入 4 个 bug 的 PR 本身成为踩坑来源 → 根因：功能验证只覆盖「happy path」（插件仓库自身），未覆盖用户项目场景（tmp_dir 跨目录、默认模式无 dloop、有历史 Done 任务）→ 教训：修改涉及路径计算或全局状态读写时，必须在边界场景（跨盘/空值/预填充数据）下验证
- [读层现象] symlink 路径问题初版只修检测未修根因 → 用户指出「建出来的就不对为什么不追查」→ 教训：broken artifact 必须追问首次创建逻辑，不能只修表面检测
