## Session 2026-05-03 18:04:59

### diwu-workflow 残留清理 → Done

**现象**：CC `/plugin` 报错 "diwu-workflow not found"，全仓库仍有 `diwu-workflow` 字符串残留。
**判断**：根因是 `~/.claude/plugins/installed_plugins.json` 中有旧 `diwu-workflow@ssdiwu` 条目（v0.4.8，指向已不存在的 `/Users/diwu/Documents/Write/Blog`）。项目自身配置文件中也有历史遗留引用。
**动作**：

### 清理项

1. **installed_plugins.json**（根因修复）：删除 `diwu-workflow@ssdiwu` 条目 → `/plugin` 错误应消除
2. **assets/dinit/assets/claude-md.template**：`diwu-workflow` → `diwu-flow`
3. **rules/README.md**（3 副本同步）：描述文字中 `diwu-workflow` → `diwu-flow`
4. **rules/verification.md**（3 副本同步）：同上

### 验证
- [x] active config 文件（plugin.json, marketplace.json, CLAUDE.md, template, 全部 rules）均无 `diwu-workflow` 残留
- [x] 仅 CHANGELOG.md / README.md 迁移章节保留历史记录（有意保留）

### 提交: 待 drec 统一 commit

### 下一步
- 无待执行任务，#58-#71 全部 Done
- v0.0.10 已发版（origin/main + public/main）

### 本次踩坑/经验

- [路由护栏契约] CC `/plugin` 报 "not found" → 根因在本地 installed_plugins.json 而非远程 marketplace → 先查本地注册表再查远程
- [验证误读] macOS `sed -i ''` 对某些正则模式静默失败 → 改用 Python `str.replace()` 可靠替换
