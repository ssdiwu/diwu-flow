## Session 2026-05-06 17:01:45
### Task#95: /simplify 路径常量集中化测试修复 — 完成

**实施内容**:
- 修复 drec_archive.py 3 处路径常量引用错误（`SETTINGS_JSON`→`DSETTINGS_JSON`, `TASK_JSON`→`DTASK_JSON`, import 行被 replace_all 误改为 `DDTASK_JSON`）
- 修复 drec_archive.py `_p()` 函数双重路径嵌套（已含 `.diwu/` 前缀的常量再拼 `DIWU_DIR`）
- 修复 dstat.py `get_tasks_summary()` 参数语义从 `diwu_dir` 改为 `cwd`，main() 中所有 `diwu_dir / CONSTANT` → `cwd / CONSTANT`
- 修复 dloop.py `_task_payload()` 参数语义从 `diwu_dir` 改为 `cwd`，cmd_start/cmd_status 中同模式修正；硬编码 `".diwu"` → `DIWU_DIR`

**验收验证**:
- [x] test_drec_archive.py 11/11 PASS（归档/幂等/踩坑聚合）
- [x] test_dstat.py 11/11 PASS（任务统计/session/归档状态/deep 模式）
- [x] 全量 pytest tests/ 325 passed, 0 failed

**提交**: 待 commit（本次 session 覆盖 Task #4/#5/#6 全部变更）

### 下一步: 无待办项，三个重构任务均已完成

### 本次踩坑/经验
- [读层现象] replace_all 替换 TASK_JSON 时误改 import 行中的 DTASK_JSON 为 DDTASK_JSON → 根因是 import 行也包含被替换子串且 replace_all 不区分上下文 → 正确做法是精确匹配或先修 import 行再批量替换单独使用处
- [分层未拆清] common.py 路径常量设计含 `.diwu/` 前缀（如 `DTASK_JSON = ".diwu/dtask.json"`），但消费者脚本仍沿用旧 `diwu_dir / CONSTANT` 模式导致双重嵌套 → 正确做法是常量含前缀时直接用 `cwd / CONSTANT`，不含前缀时才用 `diwu_dir / CONSTANT`，需统一约定并在迁移时逐文件检查
