## Session 2026-05-10 02:54:18

### 主要内容：ddoc 重构方向设计 + Skill type 字段删除 + 五方向改进

**讨论背景**：从 AIBC 项目的 /ddoc 使用体验出发，发现当前 ddoc 的"领域驱动 vs 分层"决策树在实际项目中不成立。

**实施内容**：

- 讨论并创建 issue #24（ddoc 输出结构：从领域驱动/分层到文档类型驱动）。核心发现：Curio 和 AIBC 的实际 .doc/ 都不在 ddoc 的两条路径上。
- 三路并行头脑风暴（Haiku 轻量发散 + Sonnet 深度分析 + 联网搜索），发现 Diátaxis 框架作为统一分类维度
- 联网搜索获取行业实践：Diátaxis 两轴（action/cognition × study/work）、ADR 标准
- 方向修正（507 审核）：删除 ADR/decisions（与 didea→drec 流冲突）、删除 AI 适合度标注（上一代人工文档思维）
- ddoc 定位从 L1 收束层 → L5 实施层，与 dtask/drun 并列
- ddoc 触发时机：双放方案——drun closeout 主动同步 + drec 兜底提醒
- 讨论中暴露 dpth/dref 缺多视角审议能力，创建 issue #25

- 技能 type 字段问题：探索 agent 全库扫描确认 type 三值无正式定义、无运行时效果
- 三个高星开源项目对比（planning-with-files, ui-ux-pro-max, superpowers）：均无 type 字段
- 创建并重写 issue #26：从"Skill Type 分类体系"转为"删除 type 字段及五方向改进"
- 五方向：CSO description 重构、Rigid/Flexible [R]/[S] 标记、注意力维持 hook、产品思维统一入口路由、Skill 正文结构收敛

### 当前 issue 依赖关系

```
#26（Skill 体系：删除 type + 5 方向）← 基建层，最高优先级
  └→ #24（ddoc 重构）← 依赖 #26 的 type 结论和 description 规范
  └→ #25（多视角审议 Agent）← 待讨论，依赖技能生态成熟
```

### 下一步

1. #26 方向 1-4 落地实施（纯文本改动，11 个 SKILL.md 的 frontmatter 和正文）
2. #24 ddoc 重构依据 #26 的新规范执行
3. #25 多视角审议 Agent 后续讨论

### 本次踩坑/经验

- [数据缺口] 当前 type 三值（product/rule/tool）混合了三种不同的分类维度：product 按思维层级、tool 按操作方式、rule 按文档性质。根因：type 是事后贴标签而非事前设计。 → 删除 type，分类信息保留在 skills/README.md 分组表中。
- [分层未拆清] ddoc 定位漂移：当前 CLAUDE.md 将 ddoc 归入 L1 思考收束层，但其实际价值在实施闭环（drun→ddoc 同步文档）。 → ddoc 移至 L5 实施层，与 dtask/drun 并列。
- [读层现象] 联网搜索资料（AltexSoft 等）推荐的"AI 适合度标注"反映上一代人工文档思维（文档逐渐腐化）。 → 不采纳，AI 时代应做到每版本同步更新。
