## Session 2026-05-09 17:28:29
### Task#117: PR5 说明层与表层能力模型重写（主文档） → Done
**实施内容**:
- 修复 `.doc/架构规范.md` 3 处残留：模块协作表 Commands=12→13、Commands 完整列表标题 12→13、源码仓结构树 12→13
- 修复 `.diwu/dtask.json` Task#117/Task#118 共 5 处残留：description 中 command 计数、acceptance 中 command 数量与命令列表、steps 中命令计数
- 修复 `.claude-plugin/marketplace.json`：Agent=3→5、Command=12→13
- 修复 `install.sh` 第 21 行：12 个薄壳命令→13

**验收验证**:
- [x] GWT-1: `grep "12 个 Command|12 个薄壳"` 活跃文件零匹配（archive 历史快照除外）(L3 grep 断言)
- [x] GWT-2: `python3 -m pytest tests/ -q` → 428 passed, 0 failures (L3 自动化断言)
- [x] GWT-3: plugin.json (11 skill / 13 command / 5 agent) 与所有文档交叉一致 (L3 逐项比对)

**提交**: 待本 recording commit 统一完成

### Task#118: PR5 核对验证与全量回归 → Done
**实施内容**: 全量回归通过，数字一致性最终确认

**验收验证**:
- [x] pytest 428 passed (L3)
- [x] 活跃文件 grep 零残留旧计数 (L3)

### 下一步: PR5 文档工作全部收口，可准备 commit & push

### 本次踩坑/经验
- [分层未拆清] Python 批量 regex 替换报告"0 replacements"但实际文件中确实存在目标文本 → 根因是 regex 转义或上下文不匹配导致 pattern 未命中 → 错误判断为"文件已干净无需修改" → 正确做法是直接 Read 目标行号附近内容，用 Edit 精确匹配替换而非依赖批量脚本
- [读层现象] dtask.json 的 acceptance/steps 字段是单行长 JSON 行，grep 匹配到后需精确读取该行内容再做 Edit，不能凭 grep 输出片段直接构造 old_string → 正确做法是 Read 精确 offset+limit 获取完整行内容后再 Edit
