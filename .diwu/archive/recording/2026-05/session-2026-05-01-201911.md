## Session 2026-05-01 20:19:11
### Task#55: task_entry_guard.py dloop 检查位置修正 → Done
**实施内容**: 将 `_has_active_dloop()` 检查从 `_has_active_task()` 之后移到之前。原位置 bug：真实 dloop 运行时总有 InSpec/InProgress 活跃任务，`_has_active_task` 提前 exit(0) 导致 dloop guard 永远不执行。
**验收验证**: 三分支手动复现全通过 — (1) dloop active + InSpec → BLOCK RC=1 (2) dloop inactive + InSpec → PASS RC=0 (3) dloop active + 仅 Done → BLOCK RC=1；258/258 pytest 全绿
**提交**: pending
### 下一步: 无待办
### 本次踩坑/经验
- [路由护栏契约] Guard 检查顺序即优先级：fail-fast guard 必须放在宽泛放行条件之前。_has_active_task 是"有任务就放行"的宽泛条件，dloop active 是"运行态危险"的精确拦截——精确拦截必须先于宽泛放行，否则永远不触发
- [验证误读] 手动验证脚本路径用相对路径导致 FileNotFoundError（RC=2），误判为 guard 逻辑错误 → 应先用绝对路径排除环境问题再断言业务逻辑
