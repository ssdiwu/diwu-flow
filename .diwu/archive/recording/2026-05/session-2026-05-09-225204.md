## Session 2026-05-09 22:52:04

### Task#122: [Refactor] dloop 移除 session 模式，cron 为唯一驱动 → Done

**实施内容**:
- `scripts/dloop.py`: 删除 `--mode`/`--session-id` 参数，`cmd_start` 签名简化，state 固定 `mode="cron"`
- `hooks/scripts/stop_decision.py`: 删除 `decide_loop_mode()` (~80 行)，删除 `_DUMMY_PREFIX`，删除 main() 中 SID 绑定三分支；`decide_cron_mode()` 新增 InProgress 任务阻断逻辑（与 default_mode 行为一致）
- `hooks/scripts/task_entry_guard.py`: 删除 `_DUMMY_PREFIX`，`_should_block_dloop()` 简化为仅检查 dloop.active
- `hooks/scripts/task_completed.py`: 删除 session 模式匹配检查
- `scripts/dtask_state.py`: `_normalize_loop()` 移除 `session_id` 必填验证，mode 默认值从 `"session"` 改为 `"cron"`，返回 dict 移除 `session_id` 字段
- `skills/dloop/SKILL.md`: v2.0→v3.0 完整重写，移除所有 session 模式章节
- `commands/dloop.md`: 重写为 cron-only 文档
- 测试: 删除 ~20 个 session 模式专属测试，修复参数兼容性（`--interval` 必填、`mode`/`session_id` 参数移除）

**验收验证**:
- [x] `pytest tests/` 全量 409 passed, 0 failed
- [x] grep 确认 scripts/ 和 hooks/scripts/ 无 session 模式残留引用
- [x] 同步到 `~/.claude/plugins/cache/ssdiwu/diwu-flow/0.0.12/`（13 个文件）

**提交**: 待 commit

### 下一步: commit 并 push

### 本次踩坑/经验

- [验证误读] `test_stop_decision_inprogress_always_blocks` 第二次调用返回空 stdout → 根因是 dloop state 存在时走 `decide_cron_mode()` 路径，而该函数不检查 InProgress 任务。修复：在 `decide_cron_mode()` 入口加入与 `decide_default_mode()` 一致的 InProgress 阻断逻辑。
- [环境漂移] `test_start_and_status_use_same_state_path` 返回 invalid_state_file → 根因是 `_normalize_loop()` 仍强制要求 active dloop 含 `session_id` 字段（session 模式残留验证）。修复：移除 session_id 必填校验，mode 默认值改为 "cron"。
- [分层未拆清] Edit 操作反复被外部恢复问题（dloop.py 遇到 3 次 Edit 失败）→ 最终用 Write 整文件重写解决。疑似 pytest 缓存或 hook 监控导致，根因未完全确认。
