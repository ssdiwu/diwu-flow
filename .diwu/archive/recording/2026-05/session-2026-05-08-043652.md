## Session 2026-05-08 04:36:52
### Task#96-99: PR1 规则真相源收敛 + Issue/PR 设计地图固化 → InProgress
**实施内容**:
- 完成 `#1-#8` 全量审视，按“原子变动 → 口径冲突 → issue/PR 重排”方式重构设计地图，并将 `PR1-PR5` 的定义统一回写到 `Issue #8`。
- 已创建并更新以下 issue：`#4`（testing-rule 独立真相源）、`#5`（handoff model）、`#6`（rules 原子重组）、`#7`（Commands/Skills 分层）、`#8`（总设计地图）。
- 已创建 `PR1` Draft：`https://github.com/ssdiwu/diwu-flow-dev/pull/9`，并在 `#8` 明确 `PR1` 为单个 rules/documentation PR，不拆分。
- 已对 `Issue #2/#3/#4/#6/#8` 做正文升版回灌，消除正文与评论双轨漂移：
  - `#2`：architect/debugger 稳定版口径（architect=dtask 审稿 gate，debugger=drun 异常调查第一责任 Agent）。
  - `#3`：didea 软入口/硬入口、动作门控、四层定位、上下游接口。
  - `#4`：`rules/testing.md` 必须独立、tests-first、`dtask` 消费 testing / `drun` 消费 verification。
  - `#6`：rules 原子迁移矩阵、file-layout 只面向目标项目、README 分层职责、Persistent Task 语义。
  - `#8`：issue map / PR map / `.doc`-`rules`-README 边界 / `.doc/架构规范.md` 双层重写方向。
- 已重写 `.diwu/dtask.json` 中 `Task#96-99` 的定义，使 `PR1` 的实施骨架对齐当前共识：
  - `#96`：统一三副本基线 + 新建 `rules/handoff.md` / `rules/testing.md` / `agents/README.md`
  - `#97`：收缩 `workflow.md` / `mindset.md` / `task.md` 的边界
  - `#98`：重构其余 rules 文件并升级目标项目放置与导航语义
  - `#99`：三处同步 + manifest 更新 + 全量验证收口
- 已修复一次 `.diwu/dtask.json` 编辑时引入的 JSON 分隔符错误，并用 `python3 -m json.tool` 验证 JSON 已重新合法。

**验收验证**:
- [x] `Issue #8` 已形成完整 PR Map（`PR1-PR5` 均有一句话定义、In Scope、非目标、依赖、验证方式）— 方法：GitHub issue comment 回写检查。
- [x] `Issue #2/#3/#4/#6/#8` 正文已与当前共识对齐 — 方法：逐条 issue edit / comment 回写后人工复核。
- [x] `PR1` Draft 已创建 — 方法：远端分支 `feature/pr1-rules-foundation` 经 HTTPS push 成功，`gh pr create --draft` 成功。
- [x] `.diwu/dtask.json` 已合法并含 `Task#96-99` 新定义 — 方法：`python3 -m json.tool .diwu/dtask.json` 通过。
- [ ] `PR1` 的实际文件改造尚未开始 — 原因：本次 session 主要完成设计收敛、issue 升版与任务定义重写；后续由执行型 AI 承担实现。

**提交**: commit 待本次提交生成

### 下一步: 由审查型 AI 先复核 `Issue #8` 与 `Task#96-99` 的最终边界，再由执行型 AI 按 `Task#96 → #97 → #98 → #99` 顺序落地 `PR1`

### 本次踩坑/经验
- [验证误读] 一开始把 `smoke.sh` / `task_<id>_verify.sh` 当成默认工作流结构 → 实际上它们在当前工作流中并非必需，若为可复用验证资产应进入 `tests/` 体系 → 误判为“文件存在即合理” → 正确做法是保留能力、降级文件形态：项目级基线检查和可复用任务级验证默认进入 `tests/`，不再将 `.diwu/checks/*` 视为标准结构。
- [分层未拆清] 早期把 `file-layout.md` 写成“插件源码仓结构说明” → 但它会被同步到非 diwu-flow 项目，目标读者是被接管的普通项目 → 误判为“所有放置规则都该写进 file-layout” → 正确做法是：`rules/file-layout.md` 只回答目标项目里东西放哪，插件源码仓结构规则回到 `.doc/架构规范.md`，README 只做说明与导航。
- [路由护栏契约] 早期把 `architect` 往 `drun` 里塞 → 这会让执行域承担定义域职责，破坏 handoff 模型 → 误判为“接入方便即边界正确” → 正确做法是：`architect` 固定归属 `dtask` 定义域，作为自动第三方技术审稿 gate；执行域若发现定义不稳，必须 `release -> InSpec` 回退，不得在 `drun` 内补审稿。
- [分层未拆清] `testing.md` vs `verification.md` 的职责一开始混淆 → 容易把“该写什么测试”和“当前证据是否足够完成”写在同一文件里 → 误判为“验证就是测试” → 正确做法是：`dtask` 主要消费 `testing.md` 来定义应生产什么证据，`drun` 主要消费 `verification.md` 来判定证据是否足以状态推进。

### 交接给下一位审查型 AI 的关键共识
- `.doc/` 是**项目唯一的设计文档真相源目录**；`rules/` 是运行规则真相源；各层 `README.md` 是说明/导航层，不能新增规则。
- `PR1` 是**单个** rules/documentation PR，不拆分，范围锁定为：`#5 + #6 + #4` 的 rules 真相源部分。
- `PR2` 在 `PR1` 完工后再接入：`architect` 主归属 `dtask`，`debugger` 主归属 `drun`。
- `PR3` 仅做 `dpth` + `dref/dprd` 增强式改造，不纳入 `ddoc` 重写。
- `PR4` 仅做 `didea` 本体与容器层，不混入 README / `.doc/` 重写。
- `PR5` 承接 `.doc/架构规范.md`、根 README、未来 `skills/README.md` / `commands/README.md` 与 `drun dual-entry` 的说明层结论。
- `Issue #8` 已是当前最完整的总设计地图，最新关键评论包括：
  - `PR1` 定义：`https://github.com/ssdiwu/diwu-flow-dev/issues/8#issuecomment-4400657844`
  - `PR2` 定义与依赖：`https://github.com/ssdiwu/diwu-flow-dev/issues/8#issuecomment-4400681581`
  - `PR3-PR5` 定义：`https://github.com/ssdiwu/diwu-flow-dev/issues/8#issuecomment-4400717565`
  - 本次 session 共识快照：`https://github.com/ssdiwu/diwu-flow-dev/issues/8#issuecomment-4400822612`
- 审查重点：
  1. `Task#96-99` 是否已经完整覆盖 `PR1` 边界且无越界
  2. `Issue #4/#6` 是否仍存在 tests/checks 或 plugin-vs-target-project 口径残留
  3. `Issue #1` 中 ddoc 的定位是否要进一步与 `#7` 对齐（当前先不执行，仅做审查）
