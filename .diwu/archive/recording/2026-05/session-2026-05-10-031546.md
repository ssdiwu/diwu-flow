## Session 2026-05-10 03:15:46

### Task#N: 发版机制优化 — 版本号一致性测试 → Done

**实施内容**:
- 调研业界发版方案：worktree隔离(现用) / GitHub Actions自动同步 / 单仓库导出 / git-subdir分发
- 创建 Issue #27 讨论发版机制宏观方向（短期优化 + 中期选型）
- 否决三个优化项：--dry-run（低频无需） / pytest门控（AI已跑测试） / --ci输出（过早抽象）
- 新增 `tests/level3/test_version_consistency.py`：4个测试用例校验 plugin.json / marketplace.json×2 / install.sh 版本号一致
- `drelease.sh` 保持原样不变（回退了临时加入的校验逻辑）

**验收验证**:
- [x] test_version_consistency.py 4/4 PASS (L3 自动化断言)
- [x] 全量 pytest 413 passed, 0 failed (无回归)
- [x] Issue #27 已创建: https://github.com/ssdiwu/diwu-flow-dev/issues/27

**提交**: 待 drec 统一 commit

### 下一步:
- Issue #27 持续跟踪，当发版频率增长或接受外部 PR 时考虑 GitHub Actions 方案
- CHANGELOG 追加本次变更条目

### 本次踩坑/经验

- [架构决策] 发版时校验 vs 测试时校验 → 用户正确指出版本号一致性应左移到测试层（pytest每次都跑），而非拖到发版时刻才暴露 → 正确做法：新增 level3 一致性测试，drelease.sh 保持简洁
- [环境漂移] macOS grep 不支持 `-P` (Perl regex) → 在 drelease.sh 校验逻辑中用 `grep -oP` 提取 install.sh 版本号时失败 → 改用 Python re 或 `grep -E`；最终因方案调整未采用此路径，但记录 macOS grep 差异
