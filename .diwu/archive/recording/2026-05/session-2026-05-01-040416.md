## Session 2026-05-01 04:04:16

### Task#43 补丁: _has_unlanded_plan 阈值判定从全局扫表改为只看 marker plan 自身 → Done
**实施内容**:
- 删除 _has_unlanded_plan() 中遍历 ~/.claude/plans/ 全局取 max_lines 的循环
- 替换为只读取 marker 记录的 recorded_plan 自身行数做阈值判定
- stale/不存在时清理 marker 并跳过

**验收验证**: 13/13 专项测试 + 247 passed (L3)

### Task#43 补丁: plan_exit_hint.py 缺失 file_path 时安全侧不创建 marker → Done
**实施内容**:
- 删除回退到"最近修改 .md"的逻辑（getmtime 排序）
- 无 plan_path 信息时不创建 marker（安全侧）

**验收验证**: 247 passed (L3)

### Task#45 补丁: install.sh case 子串匹配改前缀匹配 → Done
**实施内容**:
- `case "$target" in *"$FLOW_ROOT"*` → `[[ "$target" == "$FLOW_ROOT"* ]]`
- 防止 /path/to/diwu-flow-backup/skills/... 误匹配 FLOW_ROOT

**验收验证**: bash -n 通过 + 逻辑验证 (L4)

### 下一步: 无待执行任务

### 本次踩坑/经验
- [验证误读] 三次修复都指向同一模式：过宽的匹配范围（全局扫表/子串匹配/无契约字段回退）→ **正确做法**：每次修复后必须验证"不匹配边界"而非仅验证"匹配边界"
- [分层未拆清] Fix#1 的根因是阈值判定作用域错误——应该判断"这个 plan 是否够大"而不是"目录下有没有任何大文件"→ 变量命名要精确反映语义（plan_lines vs max_lines）
