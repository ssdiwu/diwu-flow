## Session 2026-05-04 02:04:08
### Task#72-74: dadr 并入 ddoc 收尾修复 + Task#75-76 补充验证 → Done
**实施内容**:
- scripts/ddoc_adr.py: 新增 _extract_status() 函数从 ADR 正文提取 Status 值；修复 _parse_readme_index() 中 re.match→re.search 编号解析 bug（match 只匹配行首导致带前缀编号丢失）；cmd_create() 状态从硬编码 Proposed 改为动态提取
- tests/level1/test_agents_config.py: 适配 Agent 模型分级验证——explorer/implementer 改为 required_model 精确匹配，verifier 保持 allowed_models 允许继承；测试断言逻辑分支处理两种模式
- .diwu/dtask-state.json: dloop 运行时元数据清空（dloop→null）；pending_recording 标记回退到 Task#74（stale 标记自愈）
- .diwu/dtask.json: Task#72/73/74 状态 InDraft→Done 修正（上一 session 遗留的状态未同步问题）
- README.md: 版本描述更新为 v0.0.11（含 dadr 并入 + Agent 分级 + verifier 终验）
**验收验证**:
- [x] pytest tests/ 全量回归通过 (L3 自动化断言)
- [x] python3 scripts/ddoc_adr.py next-number/create/update-status 功能正常 (L2 调用链)
- [x] test_agents_config.py 新增 required_model 断言逻辑覆盖 (L3 自动化断言)
**提交**: 待 commit
### 下一步: 无明确后续任务，等待新指令
### 本次踩坑/经验
- [数据缺口] dtask.json 中 Task#72-74 在上一 session 已标记 Done 但状态仍为 InDraft → release 脚本只更新 pending_recording 标记未同步 dtask.json.status → 误判为已正确更新 → 应在 release 后立即校验 dtask.json.status 与目标一致
