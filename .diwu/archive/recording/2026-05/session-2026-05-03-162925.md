## Session 2026-05-03 16:29:25

### Task#66-71: v0.0.10 精简方案 → InDraft
**实施内容**: 
- Plan→Dtask 门控修复：plan_exit_hint.py 改用 tool_input.plan（非 file_path）创建 JSON marker，task_entry_guard.py 解析 JSON marker 含 session_id + cross-session stale 清理，legacy path 兼容
- dinit sync-skills 修复：repair_kind 替代 BROKEN_PENDING 消除 double-count，symlink 目标路径基于 PLUGIN_ROOT
- dtask.json 写入 6 个新任务（66: ddoc重构, 67: 删djug, 68: 删darc+补drec command, 69: 删ddemo, 70: dref适配, 71: 全局收尾）
- stop_decision.py + dtask_state.py + dtask_transition.py: pending_recording 标记机制
- drec/drun skill: 原子 commit 入口 + 调用契约
- plugin.json/marketplace.json/install.sh/CLAUDE.md: 版本号 0.0.9 保持（未 bump）

**验收验证**: 
- [x] pytest tests/ 317 passed, 0 failed (L3)
- [x] plan_dtask_guards 21 passed (L3)
- [x] dinit tests 16 passed (L3)
- [x] python3 -m py_compile hooks/scripts/*.py scripts/*.py (L3)

**提交**: pending

### 下一步: Phase 1 ddoc 重构（Task#66）

### 本次踩坑/经验

- [路由护栏契约] ExitPlanMode hook 取 tool_input.file_path 为空 → plan_exit_hint.py 用旧 contract（file_path）判断 plan 路径，但 CC 实际传的是 tool_input.plan → marker 永远创建不了 → task_entry_guard hard block 静默失效。修复：改用 tool_input.plan 计行数 + JSON marker 含 session_id
- [验证误读] dinit sync-skills 输出 summary.total 被 BROKEN_PENDING 状态 double-count → 误判为"修复未完成" → 实际 broker 已修复，只是状态常量名不存在导致逻辑走偏。修复：用 repair_kind 统一表达
