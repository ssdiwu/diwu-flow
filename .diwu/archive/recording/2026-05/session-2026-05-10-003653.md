## Session 2026-05-10 00:36:53
### Task: 发版脚本排除敏感目录 + 公开仓库清理 → Done
**实施内容**:
- drelease.sh 清理列表加入 `.playwright-mcp` 和 `tests`，避免发版时意外推送本地工具和测试资产到公开仓库
- 追溯清理公开仓库 `public/main` 上已存在的 `.playwright-mcp/`（2 个 yml 快照文件）和 `tests/`（33 个测试文件），删除 9954 行
- 公开仓库 v0.1.0 tag 重新指向 clean commit `f6fdf05`
**提交**:
- `drelease.sh` diff: `.diwu .claude .playwright-mcp tests` 四目录清理
- 公开仓库 force push + tag 更新
### 下一步:
- 验证下次发版 `--push-public` 时不再携带上述目录
### 本次踩坑/经验
- [其他] 发版脚本清理列表维护遗漏 → 新出现的本地工具目录（.playwright-mcp）未加入清理范围 → 误以为是仓库本身问题 → 发版脚本应作为敏感目录清单的唯一下游消费点，任何新增本地工具目录都应同步审查是否需要排除
- [其他] 公开仓库 tag 更新时 annotated tag 的 SHA 与指向 commit SHA 混淆 → git ls-remote 看到的是 tag object SHA（a90d075），不是 commit SHA（f6fdf05） → 需要 `git cat-file -p` 确认指向关系，避免误判更新失败
