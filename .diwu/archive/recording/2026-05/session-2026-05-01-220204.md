## Session 2026-05-01 22:02:04
### Task#55-59: task_entry_guard dloop guard 五轮迭代修复 + 测试全覆盖 → Done
**实施内容**（5 轮迭代链）：
- Task#55 [6674a14] dloop guard 检查位置：从 _has_active_task 之后移到之前（修死代码）
- Task#56 [62b3497] 加 session_id owner/non-owner 判别（修一刀切 block）
- Task#57 [938e651] 兼容 dummy "dloop-*" SID 首轮绑定窗口（修 owner 首轮被拦）
- Task#58 [155d6b4] 硬阻止退出码 exit(1)→exit(2)（修 PreToolUse deny 语义）
- Task#59 [a60defc] 6 分支测试全覆盖 + SKILL.md dummy 窗口 tradeoff 正式记录
**验收验证**: TestDloopGuard 6 分支全绿 + 264/264 pytest 全回归；SKILL.md §已知限制落盘；plan mode 审查通过
**提交**: 6674a14, 62b3497, 938e651, 155d6b4, a60defc（本轮 5 个 commit，累计 ahead origin 12）
### 下一步: 发版准备——提交残留未提交文件（README 重写 + dtask.json 清理 + recording 195640）
### 本次踩坑/经验
- [分层未拆清] 5 轮迭代修一个 guard 函数的根因：每轮只解决"上一轮引入的回归"，没有在设计阶段穷举完整输入空间真值表。正确做法：先穷举 (dloop状态 × SID类型 × eventSID) → 标注期望行为 → 一次性实现 + 同步补测试。教训：第二轮开始就应该画真值表而不是继续打补丁
- [路由护栏契约] Hook 阻止机制是平台契约不是实现细节。PreToolUse exit(2)=deny / exit(1)=non-blocking / exit(0)=allow。前 4 轮全用 exit(1)，实际效果只是 stderr 输出但不阻止——绿色测试掩盖了假阳性。教训：改 hook 前必须先确认平台文档的退出码语义
- [路由护栏契约] 安全守卫的"已知 tradeoff"必须正式落盘不能只藏在代码注释或测试 docstring 里。dummy 窗口 foreign session 放行是 conscious design decision 不是 implicit loophole——必须写进 SKILL.md 并用测试锁定行为防止无意识变更
- [验证误读] 脚本级 returncode 断言 ≠ hook 运行时行为验证。pytest 只证明"脚本返回了什么码"，不证明"Claude Code 是否真的拦截了工具调用"。测试断言必须与平台文档的退出码语义对齐
### 错误追踪表（可选）
| 时间戳 | 工具 | 错误摘要 | 尝试 | 解决方式 | 类别 |
|--------|------|---------|------|---------|------|
| ~20:19 | Edit | guard 死代码（放 _has_active_task 后） | 1 | 移到前面 | 路由护栏契约 |
| ~20:28 | Edit | 一刀切 block（无 owner 判别） | 1 | 加 session_id 匹配 | 路由护栏契约 |
| ~20:43 | Edit | dummy SID 首轮误拦 owner | 1 | 检测 _DUMMY_PREFIX 前缀 | 路由护栏契约 |
| ~20:58 | Edit | exit(1) 不阻止（平台语义错误） | 1 | 改 exit(2) | 路由护栏契约 |
