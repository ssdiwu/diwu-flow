## Session 2026-05-03 10:03:08

### 补丁：3 项残留修复 + 1 项回归修复 + 1 项 schema 遗漏 → Done
**实施内容**: - [P0] test_validate_after_full_init NameError 崩溃：`validate_skills_src` 未定义 → 改为 `skills_src`（第532行变量名 typo） - [P1-a] .agents/skills/ 加入 .gitignore + git rm --cached 移除12个已跟踪symlink（机器特定绝对路径不可迁移） - [P1-b] dloop start 记录 initial_done_ids 快照过滤历史Done：dloop.py写入快照、_normalize_loop() schema保留字段、stop_decision fallback用快照排除历史任务 - [P2] _check_recording_reminder 覆盖untracked新文件：追加 git ls-files --others --exclude-standard - [Bonus] get_done_ids() 返回set导致JSON序列化TypeError → list()包装
**验收验证**: - [x] pytest 276 passed（全量回归） - [x] P1-b 真实链路复现：dloop start --max-tasks 1 面对历史Done(id=1)→initial_done_ids=[1]正确保留→effective_completed=0<max_tasks=1→不误停 - [x] P2 真实链路复现：untracked new_file.py + stale recording → Stop hook 输出 /drec 提醒 - [x] P1-a：git status 显示 .agents/skills/ 为 staged delete，.gitignore 已包含规则
**提交**: [待本 /drec 原子 commit]
### 下一步: 无
### 本次踩坑/经验
- [读层现象] validate_skills_src typo 导致NameError崩溃但pytest未覆盖此路径 → 根因：validate函数内部引用了外层定义的局部变量skills_src但写成了不存在的名字 → 教训：重命名/重构后必须跑一次真实CLI调用链（不只是单元测试），因为类型检查器不会捕获变量名拼写错误
- [分层未拆清] dloop state 写入端(dloop.py)和读取端(sync_runtime_state→_normalize_loop)的schema不同步 → 写入的新字段(initial_done_ids)在normalize round-trip中被静默丢弃 → 教训：state schema是写入和读取之间的契约，新增字段必须同步更新两端；否则"文件里有但代码拿不到"这类bug只能靠集成测试或人工review发现
- [验证误读] set不能JSON序列化 → get_done_ids返回set类型直接写入state dict → json.dump抛TypeError → 教训：涉及持久化的数据结构必须确认可序列化，set/list/dict的边界容易混用
