## Session 2026-05-10 18:13:58

### Task: PR #42 计划书 v1→v3 重写（Program PR + 细粒度 12 步执行计划）→ Done
**实施内容**: 
- 核查 PR #42 v1 的 5 个硬事实问题：分支 diff 范围、#28/#31 依赖冲突、manifest 已存在(JSON非YAML)、#32 消费者写错(dtask_transition不创建任务)、#31 影响面低估(实际~20消费者)
- 从"按 Issue 排列"重构为"按文件聚合排列"(Commit Group G1-G4)
- 新增细粒度执行顺序：12 个独立 push 单元，含换序灵活性规则
- PR 定位从"纯计划书"修正为"Program PR（计划+基线前置变更）"
- 测试策略从独立 G5 改为跟随对应 Group（G1/G2/G4 各带关联测试）
- #32 设计合同锁定：JSON 格式 `{next_id: N}` 复用 save_json() + `--allocate-task-id` CLI 入口
- #37 manifest 行为对齐：报错→降级全目录复制（与 Issue body 一致）
- 进度追踪表写入第九节（12 行 Step 级追踪）
**验收验证**: 
- [x] 文件交叉矩阵确认：mindset.md 三重交叉(#30+#31+#40)、4 个 hook 文件同被 #28+#31 触及
- [x] 分支实际 diff 仅 .diwu/ 文件（4 文件，37 行），与 PR 声明一致
- [x] pytest main 基线 413 passed ✅（两次确认：16.13s / 18.52s）
- [x] PR body 含完整 12 步执行顺序 + 换序灵活性规则 + 进度追踪表
**提交**: (PR draft，未开始实施代码)

### 下一步: 
- 从 Step 01 开始实施：G1-a frontmatter 批量清理(11 文件) + test_didea_layout_consistency 适配

### 本次踩坑/经验

- [读层现象] PR body 写"本分支无实现代码变更"但列了 main 上已改的文件作为"已混入基线" → reviewer 会混淆 diff 范围和 PR 声明 → 正确做法：明确标注为"Program PR（计划+基线前置变更）"，第二节逐文件列出哪些在 diff 中可见
- [分层未拆清] 测试放在独立 G5 最后统一做 → 但 G1/G2/G4 落地后 pytest 必红（test_didea_layout_consistency 还要求旧 frontmatter 字段 / test_task_166 还硬读 workflow.md） → 正确做法：测试跟随对应生产 Group 改，每个测试文件仍只改一次
- [路由护栏契约] 按 Issue 排列导致同一物理文件被多次修改（#28 先改 hooks 再 #31 改 TOML = 同一文件改两次） → 正确做法：按 Commit Group（文件聚合）排列，G4 中 #28+#31 合写每个 hook 文件只打开一次
- [验证误读] #32 Issue 写 dtask_transition.py 使用新分配器 → 实际该文件只做状态迁移不创建任务 → 正确做法：实施前必须追到真实调用链路（ID 分配入口在 /dtask 命令流），不能只看 Issue 描述就动手
